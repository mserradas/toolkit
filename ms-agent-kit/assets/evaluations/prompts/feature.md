Añade la exportación `uniqueLabels(values)` a `src/labels.mjs`. Recibe un array de strings y devuelve sus etiquetas normalizadas usando `normalizeLabel`, sin vacíos ni duplicados, en orden de primera aparición. No muta el array original. Conserva `normalizeLabel` y limita los cambios a ese módulo. Verifica con `node verify.mjs feature` y resume el resultado. No modifiques el verificador.

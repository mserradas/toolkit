Añade `canFulfill(item, quantity)` en `src/catalog.mjs`: devuelve true solo para cantidades enteras positivas que no superen stock. Integra esa función en `createOrder` de `src/order.mjs`, lanzando RangeError si no puede atender la cantidad. Conserva el error existente por producto desconocido, el resultado de pedidos válidos y el catálogo sin mutaciones. Limita cambios a esos dos módulos. Verifica con `node verify.mjs modules` y resume el resultado. No modifiques el verificador.
